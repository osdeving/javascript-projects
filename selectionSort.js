const books = require('./bookList')
const minValue = require('./minValue')

for(let current = 0; current < books.length; current++) {
    let mostCheapBook = minValue(books, current)
    let currentBook = books[current]

    

    books[current] = 

}